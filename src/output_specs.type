(
  [ DirectFile string | IndirectFile string | SyncKey string ];
  {
    file_type:
      [ RingBuf
      | Orc { with_index: bool; batch_size: u32; num_batches: u32; } ];
    fieldmask: string;  // to be parsed
    filters: { column: u32; accepted_values: $raql_value[] }[];
    channels: (u16; { timeout: float; num_sources: u16; reader_pid: u32 })[];
  }
)[]
