#!/bin/sh

set -e
top_srcdir=$(dirname $0)"/.."
. $top_srcdir/tests/funcs.sh

expected_tests=1

start
add_123
add_node n "SELECT n, lag(1, n) AS lag1, lag(2, n) AS lag2 EXPORT" 123
run

check_equal '1,1,1
2,1,1
3,2,1' "$(tail_ 3 n)"
