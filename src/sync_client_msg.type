{
  // Command sequence number:
	seq: u32;
  // Should successful commands be acknowledged with an empty error message?
	confirm_success: bool;
  // Should the change be echoed back to the client?
	echo: bool;
	cmd: $sync_client_cmd;
}
