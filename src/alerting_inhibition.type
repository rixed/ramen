{
  what: string;  // any alerts starting with this prefix
  start_date: float;  // when occurring in this time range
  stop_date: float;
  who: string;  // Who created this inhibition. Not necessarily a user, can be a soft.
  why: string;
}
