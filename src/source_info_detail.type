[ Compiled {
    default_params: $program_parameter[];
    condition: $raql_expr;
    globals: $global_variable[];
    funcs: $compiled_func[];
  }
| Failed {
    err_msg: string;
    depends_on: string; // or empty
  } ]
